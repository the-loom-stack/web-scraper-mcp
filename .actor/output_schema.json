{
  "actorSpecification": 1,
  "fields": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL that was scraped",
        "example": "https://example.com"
      },
      "title": {
        "type": "string",
        "description": "The HTML title of the scraped page",
        "example": "Example Domain"
      },
      "content": {
        "type": "string",
        "description": "Extracted page content in Markdown format, optimised for AI consumption"
      },
      "status": {
        "type": "string",
        "description": "Result status: success or error",
        "example": "success"
      },
      "error": {
        "type": "string",
        "description": "Error message if status is error, otherwise absent"
      }
    }
  },
  "views": {
    "overview": {
      "title": "Scraped Pages",
      "transformation": {
        "fields": ["url", "title", "content", "status", "error"]
      },
      "display": {
        "component": "table",
        "properties": {
          "url": { "label": "URL", "format": "link" },
          "title": { "label": "Page Title" },
          "content": { "label": "Content" },
          "status": { "label": "Status" },
          "error": { "label": "Error" }
        }
      }
    }
  }
}
