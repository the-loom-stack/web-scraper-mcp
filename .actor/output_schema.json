{
  "$schema": "https://apify.com/schemas/v1/output.ide.json",
  "actorOutputSchemaVersion": 1,
  "title": "MCP Server Output",
  "description": "This Actor runs as a persistent MCP server in Standby mode. It exposes a live MCP endpoint rather than storing results in a dataset.",
  "properties": {
    "mcpEndpoint": {
      "type": "string",
      "title": "MCP Endpoint",
      "description": "The live MCP server URL. Connect Claude Desktop, Cursor, Cline, or any MCP client to this endpoint.",
      "template": "{{run.containerUrl}}mcp"
    }
  }
}
